# Petrel 游戏系统部署指南

## 概述

本文档提供 Petrel 游戏系统的完整部署流程，包括问题诊断、正确的启动顺序和验证步骤。

## 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    外部依赖服务                          │
│  ┌──────────┐  ┌──────────┐  ┌─────────────────────┐  │
│  │  MySQL   │  │  Redis   │  │  Nacos (服务注册)   │  │
│  │  :3306   │  │  :6379   │  │      :6878          │  │
│  └──────────┘  └──────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   Petrel 核心服务                        │
│  ┌──────────────────────┐  ┌─────────────────────────┐ │
│  │ petrel-kernel-       │  │ petrel-kernel-user      │ │
│  │ register             │  │                         │ │
│  │ :7180                │  │                         │ │
│  └──────────────────────┘  └─────────────────────────┘ │
│                                                          │
│  ┌──────────────────────┐                               │
│  │ petrel-kernel-game   │                               │
│  │                      │                               │
│  └──────────────────────┘                               │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   Petrel 游戏服务                        │
│  ┌──────────────────────┐  ┌─────────────────────────┐ │
│  │ petrel-game-lobby    │  │ petrel-game-slots       │ │
│  │ :9879                │  │                         │ │
│  └──────────────────────┘  └─────────────────────────┘ │
│                                                          │
│  ┌──────────────────────┐  ┌─────────────────────────┐ │
│  │ petrel-game-chess    │  │ petrel-cms-web          │ │
│  │                      │  │                         │ │
│  └──────────────────────┘  └─────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## 前置要求

### 1. 硬件要求
- CPU: 4核心或以上
- 内存: 8GB 或以上
- 磁盘: 20GB 可用空间

### 2. 软件要求
- Java 8 或以上版本
- MySQL 5.7 或以上版本
- Redis 5.0 或以上版本

### 3. 网络端口

确保以下端口未被占用：

| 端口 | 服务 | 用途 |
|------|------|------|
| 3306 | MySQL | 数据库 |
| 6379 | Redis | 缓存 |
| 6878 | Nacos | 服务注册中心 |
| 7180 | Register | 注册服务 |
| 9879 | Lobby | 大厅服务 |

## 数据库初始化

### 1. 创建数据库

```sql
-- 创建核心数据库
CREATE DATABASE IF NOT EXISTS petrel_core 
    DEFAULT CHARACTER SET utf8mb4 
    DEFAULT COLLATE utf8mb4_unicode_ci;

-- 创建记录数据库
CREATE DATABASE IF NOT EXISTS petrel_record 
    DEFAULT CHARACTER SET utf8mb4 
    DEFAULT COLLATE utf8mb4_unicode_ci;

-- 创建 Nacos 数据库
CREATE DATABASE IF NOT EXISTS nacos 
    DEFAULT CHARACTER SET utf8mb4 
    DEFAULT COLLATE utf8mb4_unicode_ci;
```

### 2. 导入数据结构

```bash
# 导入核心数据库结构
mysql -u root -p petrel_core < /path/to/petrel_core.sql

# 导入记录数据库结构
mysql -u root -p petrel_record < /path/to/petrel_record.sql

# 导入 Nacos 数据库结构
mysql -u root -p nacos < /path/to/nacos/conf/nacos-mysql.sql
```

## 配置文件说明

### Nacos 配置

位置: `/nacos/conf/application.properties`

关键配置项：
```properties
# 端口配置
server.port=6878

# 数据库配置
spring.datasource.platform=mysql
db.num=1
db.url.0=jdbc:mysql://127.0.0.1:3306/nacos?...
db.user=root
db.password=Fagp@1908!
```

### 各服务配置

所有服务的配置都已打包在 JAR 文件中，主要配置项：

**petrel-kernel-register** (application-prod.yml):
```yaml
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:6878
```

**petrel-game-lobby** (application-prod.yml):
```yaml
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:6878

zebra:
  port: 9879
  ip:
    out: 127.0.0.1  # 外部访问 IP
    in: 127.0.0.1   # 内部监听 IP
```

## 快速启动（推荐）

### 使用改进的启动脚本

```bash
cd /path/to/petrel
./start-all-improved.sh [外部IP]

# 示例：使用默认配置 (127.0.0.1)
./start-all-improved.sh

# 示例：指定外部 IP
./start-all-improved.sh 192.168.1.100
```

脚本会自动：
1. ✅ 检查前置条件 (MySQL, Redis)
2. ✅ 启动 Nacos
3. ✅ 按正确顺序启动所有服务
4. ✅ 等待每个服务完全启动
5. ✅ 验证服务状态

### 关闭所有服务

```bash
cd /path/to/petrel
./stop-all-improved.sh
```

## 手动启动（高级）

如果需要手动控制每个服务的启动，请按以下顺序：

### 步骤 1: 启动 Nacos

```bash
cd /path/to/nacos/bin
./startup.sh -m standalone

# 等待 60 秒
sleep 60

# 验证 Nacos
curl http://127.0.0.1:6878/nacos/
```

### 步骤 2: 启动注册中心

```bash
cd /path/to/petrel
./2-.start-register.bat  # Windows
# 或
./start.sh start register  # Linux

# 等待 30 秒
sleep 30
```

### 步骤 3: 启动用户服务

```bash
./1-.start-user.bat  # Windows
# 或
./start.sh start user  # Linux

# 等待 15 秒
sleep 15
```

### 步骤 4: 启动游戏核心服务

```bash
./3-.start-game.bat  # Windows
# 或
./start.sh start game  # Linux

# 等待 15 秒
sleep 15
```

### 步骤 5: 启动大厅服务

```bash
./4-.start-lobby.bat  # Windows
# 或
./start.sh start lobby  # Linux

# 等待 15 秒
sleep 15
```

### 步骤 6: 启动其他服务

```bash
# 老虎机服务
./start.sh start slots
sleep 15

# Web 管理服务
./start.sh start web
sleep 15
```

## 验证部署

### 1. 检查进程

```bash
# 检查所有 Petrel 服务进程
ps aux | grep petrel

# 应该看到以下进程：
# - petrel-kernel-register
# - petrel-kernel-user
# - petrel-kernel-game
# - petrel-game-lobby
# - petrel-game-slots (可选)
# - petrel-cms-web (可选)
```

### 2. 检查端口

```bash
# 检查关键端口
netstat -tlnp | grep -E "6878|7180|9879"

# 应该看到：
# 6878 - Nacos
# 7180 - Register
# 9879 - Lobby
```

### 3. 检查 Nacos 服务注册

访问 Nacos 控制台：
```
http://127.0.0.1:6878/nacos/

用户名: nacos
密码: nacos
```

在"服务管理" -> "服务列表"中应该看到：
- petrel-kernel-register
- petrel-kernel-user
- petrel-kernel-game
- petrel-game-lobby
- (其他已启动的服务)

### 4. API 验证

```bash
# 查询 Lobby 服务实例
curl 'http://127.0.0.1:6878/nacos/v1/ns/instance/list?serviceName=petrel-game-lobby'

# 查询 Register 服务实例
curl 'http://127.0.0.1:6878/nacos/v1/ns/instance/list?serviceName=petrel-kernel-register'
```

### 5. 检查日志

```bash
# 查看 Lobby 服务日志
tail -f /path/to/petrel/logs/petrel-game-lobby/info_*.log

# 查看注册中心日志
tail -f /path/to/petrel/logs/petrel-kernel-register/info_*.log

# 查看 Nacos 日志
tail -f /path/to/nacos/logs/start.out
```

正常日志应包含：
- "Started [ApplicationName] in X seconds"
- "Nacos registry, [ServiceName] register finished"
- 无严重错误（ERROR）

## 常见问题排查

### 问题 1: Lobby 服务无法注册到 Register

**症状：**
- Lobby 服务启动但未在 Nacos 中显示
- 日志显示连接 Nacos 失败

**原因：**
1. Nacos 服务未启动
2. Nacos 端口配置错误
3. 服务启动顺序错误

**解决方案：**
```bash
# 1. 确认 Nacos 正在运行
ps aux | grep nacos
netstat -tlnp | grep 6878

# 2. 确认配置文件中的 Nacos 地址
# 应该是 127.0.0.1:6878

# 3. 按正确顺序重启
./stop-all-improved.sh
./start-all-improved.sh
```

### 问题 2: 服务启动后立即退出

**症状：**
- 进程启动后几秒内退出
- 无进程残留

**可能原因：**
1. 数据库连接失败
2. Redis 连接失败
3. 端口被占用
4. 配置文件错误

**解决方案：**
```bash
# 1. 检查数据库连接
mysql -h 127.0.0.1 -P 3306 -u root -p

# 2. 检查 Redis 连接
redis-cli -h 127.0.0.1 -p 6379 -a Fagp_1908

# 3. 检查端口占用
netstat -tlnp | grep -E "6878|7180|9879"

# 4. 查看详细错误日志
tail -100 /path/to/petrel/logs/*/error_*.log
```

### 问题 3: 服务间无法通信

**症状：**
- 服务都已注册但调用失败
- 超时错误

**解决方案：**
```bash
# 1. 检查服务注册状态
curl 'http://127.0.0.1:6878/nacos/v1/ns/instance/list?serviceName=petrel-game-lobby'

# 2. 检查网络连通性
telnet 127.0.0.1 9879

# 3. 检查防火墙规则
sudo iptables -L -n | grep -E "6878|7180|9879"
```

### 问题 4: 内存不足

**症状：**
- 服务启动缓慢
- OutOfMemoryError

**解决方案：**
```bash
# 1. 检查系统内存
free -h

# 2. 减少 JVM 堆内存
# 编辑启动脚本，减小 -Xms 和 -Xmx 参数
# 例如：-Xmn256m -Xms512m -Xmx512m

# 3. 分批启动服务，不使用 ALL 参数
./start.sh start register
sleep 30
./start.sh start lobby
```

## 性能优化建议

### 1. JVM 参数调优

根据服务器配置调整 JVM 参数：

```bash
# 大内存服务器 (16GB+)
-Xmn1024m -Xms2048m -Xmx2048m

# 中等内存服务器 (8GB)
-Xmn512m -Xms1024m -Xmx1024m

# 小内存服务器 (4GB)
-Xmn256m -Xms512m -Xmx512m
```

### 2. 数据库连接池

在 application-prod.yml 中调整：

```yaml
datasource:
  dynamic:
    druid:
      initial-size: 5      # 初始连接数
      min-idle: 5          # 最小空闲连接
      maxActive: 20        # 最大活动连接
```

### 3. Redis 配置

调整 Redis 内存限制：

```bash
# 在 redis.conf 中
maxmemory 2gb
maxmemory-policy allkeys-lru
```

## 监控和维护

### 日志轮转

```bash
# 定期清理旧日志
find /path/to/petrel/logs -name "*.log.*" -mtime +7 -delete
find /path/to/nacos/logs -name "*.log.*" -mtime +7 -delete
```

### 健康检查脚本

```bash
#!/bin/bash
# health-check.sh

SERVICES="petrel-kernel-register petrel-game-lobby"

for service in $SERVICES; do
    if ps aux | grep -q "[${service:0:1}]${service:1}"; then
        echo "[OK] $service is running"
    else
        echo "[FAIL] $service is not running"
        # 自动重启或发送告警
    fi
done
```

### 备份策略

```bash
# 每日备份数据库
mysqldump -u root -p petrel_core > backup_core_$(date +%Y%m%d).sql
mysqldump -u root -p petrel_record > backup_record_$(date +%Y%m%d).sql

# 每日备份配置
tar -czf config_backup_$(date +%Y%m%d).tar.gz /path/to/petrel/*.bat /path/to/nacos/conf/
```

## 升级流程

### 1. 准备升级

```bash
# 1. 备份当前版本
cp -r /path/to/petrel /path/to/petrel.backup.$(date +%Y%m%d)

# 2. 备份数据库
mysqldump -u root -p --all-databases > full_backup_$(date +%Y%m%d).sql
```

### 2. 执行升级

```bash
# 1. 停止所有服务
./stop-all-improved.sh

# 2. 替换 JAR 文件
cp new_version/*.jar /path/to/petrel/

# 3. 更新数据库结构（如果需要）
mysql -u root -p < upgrade_scripts/v2.0.sql
```

### 3. 验证升级

```bash
# 1. 启动服务
./start-all-improved.sh

# 2. 检查版本
# 查看日志中的版本信息

# 3. 功能测试
# 执行关键业务流程测试
```

## 安全建议

1. **修改默认密码**
   - Nacos 控制台密码
   - MySQL root 密码
   - Redis 密码

2. **配置防火墙**
   ```bash
   # 只允许必要的端口
   sudo ufw allow 6878/tcp  # Nacos
   sudo ufw allow 7180/tcp  # Register
   sudo ufw allow 9879/tcp  # Lobby
   ```

3. **启用 SSL/TLS**
   - 为 Nacos、MySQL、Redis 配置 SSL

4. **日志审计**
   - 定期审查访问日志
   - 监控异常访问模式

## 技术支持

如有问题，请查看：
- 分析报告: `分析报告-lobby模块注册问题.md`
- 服务日志: `/path/to/petrel/logs/`
- Nacos 日志: `/path/to/nacos/logs/`

---

**文档版本**: 1.0  
**最后更新**: 2025-10-28  
**适用系统**: Petrel Gaming Platform v1.0
